// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages the SCIM configuration for a dbt Cloud account. SCIM (System for Cross-domain Identity Management) allows identity providers such as Okta and Azure AD to automatically provision and deprovision users and groups.
 *
 * This is a singleton resource — only one SCIM configuration exists per account. Destroying the resource resets the configuration to its defaults (SCIM disabled, manual updates allowed, license type not SCIM-controlled).
 *
 * Requires the SCIM feature to be enabled on the account (enterprise plans only).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dbtcloud from "@pulumi/dbtcloud";
 *
 * // Enable SCIM with strict IdP control — no manual updates, license type managed by SCIM.
 * // This is the recommended configuration when using an identity provider such as
 * // Okta or Azure AD as the single source of truth for user provisioning.
 * const main = new dbtcloud.ScimConfig("main", {
 *     enabled: true,
 *     manualUpdatesAllowed: false,
 *     scimControlledLicenseType: true,
 * });
 * ```
 *
 * ## Import
 *
 * Singleton resource — import using the dbt Cloud account ID.
 *
 * ```sh
 * $ pulumi import dbtcloud:index/scimConfig:ScimConfig main 12345
 * ```
 */
export class ScimConfig extends pulumi.CustomResource {
    /**
     * Get an existing ScimConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ScimConfigState, opts?: pulumi.CustomResourceOptions): ScimConfig {
        return new ScimConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'dbtcloud:index/scimConfig:ScimConfig';

    /**
     * Returns true if the given object is an instance of ScimConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ScimConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ScimConfig.__pulumiType;
    }

    /**
     * Whether SCIM provisioning is enabled for the account.
     */
    declare public readonly enabled: pulumi.Output<boolean>;
    /**
     * Whether administrators can manually update users and groups that are managed by SCIM. When set to ~~~false~~~, SCIM is the sole source of truth for user and group management.
     */
    declare public readonly manualUpdatesAllowed: pulumi.Output<boolean>;
    /**
     * Whether the dbt Cloud license type (Developer, Read-Only, IT) is controlled by SCIM attribute mapping. When set to ~~~false~~~, license types are managed manually inside dbt Cloud.
     */
    declare public readonly scimControlledLicenseType: pulumi.Output<boolean>;

    /**
     * Create a ScimConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ScimConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ScimConfigArgs | ScimConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ScimConfigState | undefined;
            resourceInputs["enabled"] = state?.enabled;
            resourceInputs["manualUpdatesAllowed"] = state?.manualUpdatesAllowed;
            resourceInputs["scimControlledLicenseType"] = state?.scimControlledLicenseType;
        } else {
            const args = argsOrState as ScimConfigArgs | undefined;
            if (args?.enabled === undefined && !opts.urn) {
                throw new Error("Missing required property 'enabled'");
            }
            if (args?.manualUpdatesAllowed === undefined && !opts.urn) {
                throw new Error("Missing required property 'manualUpdatesAllowed'");
            }
            if (args?.scimControlledLicenseType === undefined && !opts.urn) {
                throw new Error("Missing required property 'scimControlledLicenseType'");
            }
            resourceInputs["enabled"] = args?.enabled;
            resourceInputs["manualUpdatesAllowed"] = args?.manualUpdatesAllowed;
            resourceInputs["scimControlledLicenseType"] = args?.scimControlledLicenseType;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ScimConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ScimConfig resources.
 */
export interface ScimConfigState {
    /**
     * Whether SCIM provisioning is enabled for the account.
     */
    enabled?: pulumi.Input<boolean>;
    /**
     * Whether administrators can manually update users and groups that are managed by SCIM. When set to ~~~false~~~, SCIM is the sole source of truth for user and group management.
     */
    manualUpdatesAllowed?: pulumi.Input<boolean>;
    /**
     * Whether the dbt Cloud license type (Developer, Read-Only, IT) is controlled by SCIM attribute mapping. When set to ~~~false~~~, license types are managed manually inside dbt Cloud.
     */
    scimControlledLicenseType?: pulumi.Input<boolean>;
}

/**
 * The set of arguments for constructing a ScimConfig resource.
 */
export interface ScimConfigArgs {
    /**
     * Whether SCIM provisioning is enabled for the account.
     */
    enabled: pulumi.Input<boolean>;
    /**
     * Whether administrators can manually update users and groups that are managed by SCIM. When set to ~~~false~~~, SCIM is the sole source of truth for user and group management.
     */
    manualUpdatesAllowed: pulumi.Input<boolean>;
    /**
     * Whether the dbt Cloud license type (Developer, Read-Only, IT) is controlled by SCIM attribute mapping. When set to ~~~false~~~, license types are managed manually inside dbt Cloud.
     */
    scimControlledLicenseType: pulumi.Input<boolean>;
}
