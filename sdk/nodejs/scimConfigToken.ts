// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages a SCIM API token for a dbt Cloud account. SCIM tokens are used by identity providers (e.g. Okta, Azure AD) to provision and deprovision users and groups automatically.
 *
 * The token value is only available immediately after creation and is stored in Terraform state as a sensitive value. It cannot be retrieved from the API afterwards.
 *
 * Requires the SCIM feature to be enabled on the account (enterprise plans only).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dbtcloud from "@pulumi/dbtcloud";
 *
 * // Create a SCIM token to allow an identity provider (e.g. Okta, Azure AD) to
 * // provision and deprovision users and groups in dbt Cloud.
 * //
 * // The token value is returned only on creation and stored in Terraform state
 * // as a sensitive value. It is never returned by subsequent API reads, so
 * // changing the `name` forces a new token to be created.
 * const okta = new dbtcloud.ScimConfigToken("okta", {name: "okta-scim"});
 * export const scimToken = okta.tokenString;
 * ```
 *
 * ## Import
 *
 * Import a SCIM config token by its numeric ID.
 * Note: tokenString will be empty after import — the API never returns the
 * token value after creation. This is expected and documented behaviour.
 *
 * ```sh
 * $ pulumi import dbtcloud:index/scimConfigToken:ScimConfigToken okta 12345
 * ```
 */
export class ScimConfigToken extends pulumi.CustomResource {
    /**
     * Get an existing ScimConfigToken resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ScimConfigTokenState, opts?: pulumi.CustomResourceOptions): ScimConfigToken {
        return new ScimConfigToken(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'dbtcloud:index/scimConfigToken:ScimConfigToken';

    /**
     * Returns true if the given object is an instance of ScimConfigToken.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ScimConfigToken {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ScimConfigToken.__pulumiType;
    }

    /**
     * Timestamp when the token was created.
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    /**
     * Timestamp when the token was last used. Null if never used.
     */
    declare public /*out*/ readonly lastUsed: pulumi.Output<string>;
    /**
     * A human-readable name for the token. Changing this value forces a new token to be created.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The SCIM token value. Only available immediately after creation — not returned by subsequent API reads. Store this value securely.
     */
    declare public /*out*/ readonly tokenString: pulumi.Output<string>;

    /**
     * Create a ScimConfigToken resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: ScimConfigTokenArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ScimConfigTokenArgs | ScimConfigTokenState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ScimConfigTokenState | undefined;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["lastUsed"] = state?.lastUsed;
            resourceInputs["name"] = state?.name;
            resourceInputs["tokenString"] = state?.tokenString;
        } else {
            const args = argsOrState as ScimConfigTokenArgs | undefined;
            resourceInputs["name"] = args?.name;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["lastUsed"] = undefined /*out*/;
            resourceInputs["tokenString"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["tokenString"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(ScimConfigToken.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ScimConfigToken resources.
 */
export interface ScimConfigTokenState {
    /**
     * Timestamp when the token was created.
     */
    createdAt?: pulumi.Input<string>;
    /**
     * Timestamp when the token was last used. Null if never used.
     */
    lastUsed?: pulumi.Input<string>;
    /**
     * A human-readable name for the token. Changing this value forces a new token to be created.
     */
    name?: pulumi.Input<string>;
    /**
     * The SCIM token value. Only available immediately after creation — not returned by subsequent API reads. Store this value securely.
     */
    tokenString?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a ScimConfigToken resource.
 */
export interface ScimConfigTokenArgs {
    /**
     * A human-readable name for the token. Changing this value forces a new token to be created.
     */
    name?: pulumi.Input<string>;
}
