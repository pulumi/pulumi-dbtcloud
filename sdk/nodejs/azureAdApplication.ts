// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages an Azure Active Directory (Microsoft Entra ID) application registration for a dbt Cloud account. This enables Azure DevOps integration, allowing dbt Cloud to access Azure DevOps repositories for project setup.
 *
 * The `clientId`, `clientSecret` and `tenantId` are encrypted at rest and never returned by the API. They are stored as sensitive values in Terraform state so they can be resent on every update — the API requires all three on both create and update.
 *
 * **Destroy behaviour:** running `terraform destroy` calls the dbt Cloud DELETE endpoint, which marks the record as inactive. Due to a known dbt Cloud backend limitation, the underlying database row is retained and re-creating the resource against the same account without a backend cleanup will fail with a unique-constraint error. If you need to recreate the resource after a destroy, contact dbt Cloud support to have the orphaned record removed, or use `pulumi import` to re-adopt the existing record ID.
 *
 * Requires the Azure DevOps integration feature to be enabled on the account (enterprise plans only).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dbtcloud from "@pulumi/dbtcloud";
 *
 * const _this = new dbtcloud.AzureAdApplication("this", {
 *     organizationName: "my-azure-devops-org",
 *     clientId: "00000000-0000-0000-0000-000000000000",
 *     clientSecret: azureClientSecret,
 *     tenantId: "00000000-0000-0000-0000-000000000001",
 *     azureServiceAuthenticationMethod: "service_user",
 * });
 * ```
 *
 * ## Import
 *
 * using import blocks (requires Terraform >= 1.5)
 * import {
 * to = dbtcloud_azure_ad_application.this
 * id = "azureAdApplicationId"
 * }
 *
 * import {
 * to = dbtcloud_azure_ad_application.this
 * id = "12345"
 * }
 *
 * using the older import command
 *
 * ```sh
 * $ pulumi import dbtcloud:index/azureAdApplication:AzureAdApplication this azure_ad_application_id
 * $ pulumi import dbtcloud:index/azureAdApplication:AzureAdApplication this 12345
 * ```
 *
 * NOTE: client_id, client_secret, and tenantId will be empty after import —
 * the API never returns these values. You must set them in your config to
 * avoid drift on the next apply.
 *
 * Import is also the recovery path if destroy left an orphaned record in dbt
 * Cloud (the DELETE endpoint soft-deletes the row rather than removing it).
 * Find the existing record ID and import it instead of creating a new one.
 */
export class AzureAdApplication extends pulumi.CustomResource {
    /**
     * Get an existing AzureAdApplication resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AzureAdApplicationState, opts?: pulumi.CustomResourceOptions): AzureAdApplication {
        return new AzureAdApplication(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'dbtcloud:index/azureAdApplication:AzureAdApplication';

    /**
     * Returns true if the given object is an instance of AzureAdApplication.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AzureAdApplication {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AzureAdApplication.__pulumiType;
    }

    /**
     * The ID of the dbt Cloud account.
     */
    declare public /*out*/ readonly accountId: pulumi.Output<number>;
    /**
     * The method used for service authentication. One of: ~~~service*user~~~, ~~~service*principal~~~. Defaults to ~~~service_user~~~.
     */
    declare public readonly azureServiceAuthenticationMethod: pulumi.Output<string>;
    /**
     * The client ID (application ID) of the Azure AD app registration. Stored as a sensitive value — the API never returns it.
     */
    declare public readonly clientId: pulumi.Output<string>;
    /**
     * The client secret of the Azure AD app registration. Stored as a sensitive value — the API never returns it.
     */
    declare public readonly clientSecret: pulumi.Output<string>;
    /**
     * Timestamp when the application was created.
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    /**
     * The domain used for the OAuth redirect URI. Set automatically by dbt Cloud based on the account's subdomain.
     */
    declare public /*out*/ readonly oauthRedirectUriDomain: pulumi.Output<string>;
    /**
     * The name of the Azure DevOps organization.
     */
    declare public readonly organizationName: pulumi.Output<string>;
    /**
     * The tenant ID of the Azure AD directory. Stored as a sensitive value — the API never returns it.
     */
    declare public readonly tenantId: pulumi.Output<string>;
    /**
     * Timestamp when the application was last updated.
     */
    declare public /*out*/ readonly updatedAt: pulumi.Output<string>;

    /**
     * Create a AzureAdApplication resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AzureAdApplicationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AzureAdApplicationArgs | AzureAdApplicationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AzureAdApplicationState | undefined;
            resourceInputs["accountId"] = state?.accountId;
            resourceInputs["azureServiceAuthenticationMethod"] = state?.azureServiceAuthenticationMethod;
            resourceInputs["clientId"] = state?.clientId;
            resourceInputs["clientSecret"] = state?.clientSecret;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["oauthRedirectUriDomain"] = state?.oauthRedirectUriDomain;
            resourceInputs["organizationName"] = state?.organizationName;
            resourceInputs["tenantId"] = state?.tenantId;
            resourceInputs["updatedAt"] = state?.updatedAt;
        } else {
            const args = argsOrState as AzureAdApplicationArgs | undefined;
            if (args?.clientId === undefined && !opts.urn) {
                throw new Error("Missing required property 'clientId'");
            }
            if (args?.clientSecret === undefined && !opts.urn) {
                throw new Error("Missing required property 'clientSecret'");
            }
            if (args?.organizationName === undefined && !opts.urn) {
                throw new Error("Missing required property 'organizationName'");
            }
            if (args?.tenantId === undefined && !opts.urn) {
                throw new Error("Missing required property 'tenantId'");
            }
            resourceInputs["azureServiceAuthenticationMethod"] = args?.azureServiceAuthenticationMethod;
            resourceInputs["clientId"] = args?.clientId ? pulumi.secret(args.clientId) : undefined;
            resourceInputs["clientSecret"] = args?.clientSecret ? pulumi.secret(args.clientSecret) : undefined;
            resourceInputs["organizationName"] = args?.organizationName;
            resourceInputs["tenantId"] = args?.tenantId ? pulumi.secret(args.tenantId) : undefined;
            resourceInputs["accountId"] = undefined /*out*/;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["oauthRedirectUriDomain"] = undefined /*out*/;
            resourceInputs["updatedAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["clientId", "clientSecret", "tenantId"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(AzureAdApplication.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AzureAdApplication resources.
 */
export interface AzureAdApplicationState {
    /**
     * The ID of the dbt Cloud account.
     */
    accountId?: pulumi.Input<number>;
    /**
     * The method used for service authentication. One of: ~~~service*user~~~, ~~~service*principal~~~. Defaults to ~~~service_user~~~.
     */
    azureServiceAuthenticationMethod?: pulumi.Input<string>;
    /**
     * The client ID (application ID) of the Azure AD app registration. Stored as a sensitive value — the API never returns it.
     */
    clientId?: pulumi.Input<string>;
    /**
     * The client secret of the Azure AD app registration. Stored as a sensitive value — the API never returns it.
     */
    clientSecret?: pulumi.Input<string>;
    /**
     * Timestamp when the application was created.
     */
    createdAt?: pulumi.Input<string>;
    /**
     * The domain used for the OAuth redirect URI. Set automatically by dbt Cloud based on the account's subdomain.
     */
    oauthRedirectUriDomain?: pulumi.Input<string>;
    /**
     * The name of the Azure DevOps organization.
     */
    organizationName?: pulumi.Input<string>;
    /**
     * The tenant ID of the Azure AD directory. Stored as a sensitive value — the API never returns it.
     */
    tenantId?: pulumi.Input<string>;
    /**
     * Timestamp when the application was last updated.
     */
    updatedAt?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a AzureAdApplication resource.
 */
export interface AzureAdApplicationArgs {
    /**
     * The method used for service authentication. One of: ~~~service*user~~~, ~~~service*principal~~~. Defaults to ~~~service_user~~~.
     */
    azureServiceAuthenticationMethod?: pulumi.Input<string>;
    /**
     * The client ID (application ID) of the Azure AD app registration. Stored as a sensitive value — the API never returns it.
     */
    clientId: pulumi.Input<string>;
    /**
     * The client secret of the Azure AD app registration. Stored as a sensitive value — the API never returns it.
     */
    clientSecret: pulumi.Input<string>;
    /**
     * The name of the Azure DevOps organization.
     */
    organizationName: pulumi.Input<string>;
    /**
     * The tenant ID of the Azure AD directory. Stored as a sensitive value — the API never returns it.
     */
    tenantId: pulumi.Input<string>;
}
